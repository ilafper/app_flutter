import 'package:flutter/material.dart';

class sql_Home extends StatelessWidget{
  const sql_Home({super.key});



  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text("Vista SQL")),
      body: Center(
        child: Text("BIENVENIDO VISTA SQL"),
      ),
    );
  }
}